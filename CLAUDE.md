# {프로젝트명}

## 프로젝트 컨텍스트

시작하기 전에 아래 문서들을 반드시 읽어라:
- `/docs/PRD.md` — 제품 요구사항 (목표, 사용자, 핵심 기능)
- `/docs/ARCHITECTURE.md` — 아키텍처 (디렉토리 구조, 패턴, 데이터 흐름)
- `/docs/ADR.md` — 기술 결정 기록 (결정, 이유, 트레이드오프)
- `/docs/MIGRATION.md` — 기존 프로젝트에 템플릿 적용 방법

이 문서들은 프로젝트의 기획·아키텍처·설계 의도를 담고 있으며, 모든 구현은 이 문서들의 방향성을 따라야 한다.
새로운 기술 결정이 필요하면 ADR.md에 기록하라.

## 프로젝트 규칙

`claude/rules/*.md` 파일들이 자동으로 로드된다. 다음 규칙을 준수하라:
- `coding-style.md` — 코딩 스타일, 가독성, 불변성
- `karpathy-guidelines.md` — 생각 후 코딩, 단순함, 수술적 변경
- `testing.md` — TDD, 80% 커버리지, 에지 케이스
- `security.md` — 시크릿 관리, 입력 검증, 인가
- `performance.md` — 알고리즘 효율, 모델 선택
- `git-workflow.md` — 커밋 컨벤션, 브랜치 전략
- `onboarding.md` — clone → 실행 단계별 가이드
- `api-conventions.md` — REST API 설계 규칙
- `data-model.md` — 스키마, 마이그레이션, 관계 설계

`claude/examples/` 디렉토리에 프레임워크별 아키텍처 예시가 있다:
- `fullstack-next-nestjs.md` — Next.js 16 + NestJS v11 풀스택 예시

## 기술 스택

{기술 스택 나열 — 예: Next.js 16 (프론트), NestJS v11 (백엔드), TypeScript, Tailwind CSS v4, PostgreSQL, Supabase}

## 개발 명령어

```bash
# 개발 서버
{프로젝트별 명령어 — 예: npm run dev, python main.py, cargo run}

# 빌드
{프로젝트별 명령어 — 예: npm run build, uv build, cargo build}

# 테스트
{프로젝트별 명령어 — 예: npm test, pytest, cargo test}

# 린트
{프로젝트별 명령어 — 예: npm run lint, ruff check, cargo clippy}
```

## 하네스 실행

이 템플릿은 두 가지 방식으로 실행한다:

### 1. 자동 실행 (execute.py)

Phase 단위로 step을 순차 자동 실행. 한 번 시작하면 완료까지 진행:

```bash
# phase 전체 실행
python3 scripts/execute.py 0-mvp

# 완료 후 push 포함
python3 scripts/execute.py 0-mvp --push
```

- 자동 3회 재시도, per-step 커밋, branch 자동 생성
- 상태 파일: `phases/{phase-name}/index.json`
- 실패 시: index.json의 status를 `pending`으로 리셋 후 재실행

### 2. 수동 작업 (OMC / Claude Code 대화형)

step을 하나씩 골라서 깊이 논의하며 진행:

```
"Step 1 (core)부터 시작해줘. ARCHITECTURE.md 읽고 구현해줘."
"이 부분 더 자세히 논의하고 싶어. trade-off 알려줘."
```

- OMC 모드: `ultrawork`, `team`, `ralph` 등으로 작업 분해 가능
- execute.py보다 유연하지만 매번 지시 필요

## 작업 방식

### 0. 아키텍처 판단 (작업 시작 전)

요구사항을 듣고 **가장 단순한 아키텍처**를 선택하라. 아래 기준으로 판단:

- 백엔드가 필요 없으면? → 프론트엔드 전용 (BaaS: Supabase, Firebase)
- 인증/결제/복잡한 로직 없으면? → Lean Mode
- PoC/프로토타입이면? → Lean Mode (1-2주 내 배포 목표)
- 풀스택이 진짜 필요하면? → 풀스택 모드 (ADR에 이유 기록)

**원칙**: 과한 아키텍처는 기술 부채다. 필요하면 확장해라, 미리 만들지 마라.

### 0-1. 아키텍처 제안 및 사용자 승인

선택한 아키텍처를 사용자에게 **제안**하라. 아래 포맷으로 설명해야 한다:

1. **왜 이 아키텍처인가**: 요구사항과 매핑한 근거
2. **장점**: 이 선택이 주는 이점 (속도, 비용, 유지보수 등)
3. **단점**: 포기하는 것, 미래에 생길 수 있는 문제
4. **Trade-off**: 다른 선택지와 비교표

```
추천 아키텍처: {옵션명}

왜: {요구사항 분석 → 아키텍처 매핑 근거}

장점:
- {장점 1}
- {장점 2}

단점:
- {단점 1}
- {단점 2}

Trade-off 비교:
| 옵션 | 속도 | 확장성 | 운영비용 | 적합도 |
|------|------|--------|----------|--------|
| 추천안 | 빠름 | 보통 | 낮음 | ★★★★★ |
| 대안A | 보통 | 높음 | 높음 | ★★★ |
| 대안B | 느림 | 매우높음 | 매우높음 | ★★
```

사용자가 승인하면 진행. 거부하면 대안 제시 후 재논의.

### 1. 탐색
`docs/` 문서를 읽고 프로젝트의 기획·아키텍처·설계 의도를 파악한다.

### 2. 논의
구현을 위해 구체화하거나 기술적으로 결정해야 할 사항이 있으면 사용자에게 제시하고 논의한다.

### 3. 분해
작업을 여러 step으로 쪼개어 초안을 작성하고 피드백을 요청한다.

**설계 원칙:**
- **Scope 최소화** — 하나의 step에서 하나의 레이어 또는 모듈만
- **자기완결성** — 각 step은 독립적으로 실행 가능해야 함
- **시그니처 수준 지시** — 인터페이스만 제시, 내부 구현은 재량
- **AC는 실행 가능한 커맨드** — `npm run build && npm test` 같은 실제 검증 명령
- **네이밍** — step name은 kebab-case (`project-setup`, `api-layer`)

### 4. 실행
각 step을 독립적으로 구현한다.

### 5. 검증
각 step 완료 시 아래 체크리스트로 검증한다. 위반 사항이 있으면 수정 방안을 구체적으로 제시하라.

## 아키텍처 원칙

**단순함 + 견고성.** 필요하면 확장해라, 미리 만들지 마라.

- **Lean first**: 가장 단순한 아키텍처로 시작. 과한 설계는 기술 부채다
- **비즈니스 로직은 순수 함수로** — 테스트 쉽고 예측 가능
- **상태/엔티티는 클래스/인터페이스로** — 타입 안전하고 명확
- **비즈니스 변경에 유연하게** — 도메인별 분리, 느슨한 결합
- **복잡함은 내부에, 간단한 인터페이스는 외부에**

## 아키텍처 단순화 명령

요구사항과 아키텍처 복잡도가 충돌할 때, 아래 기준으로 **판단하고 단순화안을 사용자에게 제안**하라. **무단 적용 금지** — 항상 설명 후 승인받는다.

### 단순화 트리거

아래 조건 중 하나라도 해당되면 단순화 모드를 활성화하라:

1. **백엔드 과잉**: 인증/결제/복잡한 로직이 없는데 별도 백엔드를 구성 중 → BaaS로 전환
2. **모듈 과잉**: 파일 < 20개인데 도메인별 모듈 분리 중 → 단일 디렉토리로 통합
3. **인증 과잉**: PoC/MVP에 JWT + OAuth + Refresh Token 구성 중 → 세션 기반 또는 BaaS 인증으로 단순화
4. **DB 과잉**: 데이터 < 100건, 관계 단순한데 PostgreSQL + 마이그레이션 구성 중 → SQLite 또는 JSON
5. **테스트 과잉**: 핵심 기능 1-2개에 E2E + 통합 + 단위 테스트 전체 구성 중 → 핵심 E2E 1-2개로 축소
6. **배포 과잉**: 1-2주 내 배포 목표인데 Kubernetes + CI/CD 구성 중 → Vercel/Render 원클릭 배포

### 단순화 제안 포맷

단순화가 필요할 때 반드시 아래 포맷으로 사용자에게 제시하라:

```
⚠️ 아키텍처 단순화 제안

현재 구성: {현재 아키텍처 설명}
문제점: {왜 과한지 — 요구사항과 불일치 지점}

제안: {단순화된 아키텍처}

왜:
- {근거 1}
- {근거 2}

Trade-off:
| | 현재안 | 단순화안 |
|--|--------|----------|
| 개발기간 | {기간} | {기간} |
| 파일수 | {수} | {수} |
| 확장성 | {높음/보통/낮음} | {높음/보통/낮음} |
| 운영비용 | {비용} | {비용} |
| 미래리스크 | {리스크} | {리스크} |

장점: {단순화의 이점}
단점: {포기하는 것 + 언제 문제가 되는가}
언제 전환해야 하는가: {확장이 필요한 트리거 조건}
```

### 단순화 원칙

- **제거가 최고 최적화다** — 코드 100줄 줄이는 게 10줄 최적화보다 낫다
- **BaaS가 답인 경우가 많다** — Supabase, Firebase, Clerk, Vercel 등
- **분리는 아픔이 생길 때 한다** — 아픔이 없으면 분리하지 마라
- **Lean에서 시작하는 게 실패 리스크가 낮다** — 확장은 쉽지만, 축소는 어렵다

## 리뷰 체크리스트

| 항목 | 결과 | 비고 |
|------|------|------|
| 아키텍처 준수 | ✅/❌ | ARCHITECTURE.md에 정의된 디렉토리 구조를 따르는가? |
| 기술 스택 준수 | ✅/❌ | ADR에 정의된 기술 선택을 벗어나지 않았는가? |
| 테스트 존재 | ✅/❌ | 새로운 기능에 대한 테스트가 작성되어 있는가? |
| CRITICAL 규칙 | ✅/❌ | 시크릿 미노출, 입력 검증, 불변성, 에러 삼킴 없음 |
| 빌드 가능 | ✅/❌ | 빌드 명령어가 에러 없이 통과하는가? |

위반 사항이 있으면 수정 방안을 구체적으로 제시하라.

## [CUSTOMIZE] 프로젝트별 특이사항

프로젝트 고유 규칙이나 특이사항을 여기에 추가
